import BottomMenuElement from "@components/BottomMenu/BottomMenuElement.tsx";
import styles from "@styles/components/BottomMenu/BottomMenu.module.css";

const BottomMenu = () => {
	return (
		<menu className={styles.menu}>

		</menu>
	);
};

export default BottomMenu;