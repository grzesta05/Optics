import { Container, MenuLink } from "@components";

function Start() {
  return (
    <Container>
      <h1>Start the app!</h1>
      <MenuLink href={"/"}>Back</MenuLink>
    </Container>
  );
}

export default Start;
